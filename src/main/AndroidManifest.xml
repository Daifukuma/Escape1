<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.escape">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Next3"></activity>
        <activity android:name=".Last" />
        <activity android:name=".Escape04" />
        <activity android:name=".Next2" />
        <activity android:name=".Next" />
        <activity android:name=".Loading" />
        <activity android:name=".Escape03" />
        <activity android:name=".Escape02" />
        <activity android:name=".Escape01" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>